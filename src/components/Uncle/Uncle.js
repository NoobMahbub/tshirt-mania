import React from 'react';

const Uncle = ({house}) => {
    return (
        <div>
            <h2>Hello from uncle</h2>
            <p>House:{house}</p>
        </div>
    );
};

export default Uncle;